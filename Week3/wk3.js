let ages = [3, 9, 23, 64, 2, 8, 28, 93]; {
    console.log(ages.length);
}
let lastElement = ages[ages.length - 1];
console.log(lastElement);
let firstElement = (3);
console.log(firstElement);
console.log(lastElement - firstElement);

let.push (33); {
   console.log(ages.push);
}
console.log(ages);
for(i = 0; i < ages.length; i++) {
   if (ages[i] = 3 + 9 + 23 + 64 + 2 + 8 + 28 + 93 + 33) {
       console.log("you got the sum")
   }
console.log(ages [i]); {
}
}
function findAverage(ages){
   let average = 0;
   for(let i = 0; i < ages.length; i++){
      console.log (ages.length);`
       average += sum;`
   }
   average = ages[i] / ages.length;
   return average;
}
findAverage([3, 9, 23, 64, 2, 8, 28, 93, 33]);

findsumOfLetters = ['Sam', 'Tommy', 'Tim', 'Sally', 'Buck', 'Bob'];
   for (let i = 0; i < names.length; i++) {
       console.log (names.length);
       names += sumOfLetters;
       }
       sumOfLetters = names[i] / names.length